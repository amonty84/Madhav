SESSION CLOSE — M2_D7_PER_TOOL_PLANNER — 2026-04-30T17:10:00Z

Pre-flight findings:
  Haiku model constant: TITLE_MODEL_ID = 'claude-haiku-4-5' (in @/lib/models/registry; no HAIKU_MODEL_ID)
    → Used resolveModel(TITLE_MODEL_ID) following the pattern in router.ts, route.ts, etc.
  AI SDK properties: maxOutputTokens (not maxTokens), inputTokens/outputTokens (not promptTokens/completionTokens)
  Feature flag pattern: FeatureFlag union type + DEFAULT_FLAGS record (both must be updated)
  RETRIEVAL_TOOLS count confirmed: 17
  Trace step_type union: 'deterministic' | 'llm' | 'sql' | 'vector' | 'gcs' — 'plan' NOT added
    → Adding 'plan' would break platform/src/components/trace/TracePanel.tsx (must_not_touch)
    → Used 'llm' for plan_per_tool step_type (appropriate: step makes parallel Haiku calls)
  Vitest baseline: 1015 passing / 13 failing (pre-existing known residuals)
  parallel_group: field is string | undefined — null not assignable; used undefined/omitted

ACs result:
  AC.1:  PASS — branch redesign/r0-foundation
  AC.2:  PASS — per_tool_planner.ts created
  AC.3:  PASS — 17 prompt templates present (grep count = 17)
  AC.4:  PASS — PER_TOOL_PLANNER_ENABLED flag added (default false)
  AC.5:  PASS — consume/route.ts wired (import + call + trace) — 10 references total
  AC.6:  PASS — plan_per_tool step emitted in both flag=true and flag=false paths (4 occurrences; 2 for step_name field)
  AC.7:  PASS — TypeScript compiles clean (no new errors; 9 pre-existing errors in must_not_touch test files)
  AC.8:  PASS — per-tool planner tests: 15/15 passing (exceeds ≥12 requirement)
  AC.9:  PASS — no new vitest failures (13 failures unchanged; 1047 passing, was 1015)
  AC.10: PASS — Cloud Run revision amjis-web-00039-hzk deployed and serving 100% traffic
  AC.11: CODE-PASS — plan_per_tool step emitted with planner_active=false in else branch (deterministic)
         Live smoke requires native to issue a Consume query and check trace SSE stream
  AC.12: DEFERRED to native — requires MARSYS_FLAG_PER_TOOL_PLANNER_ENABLED=true env override

Files created/modified:
  platform/src/lib/router/per_tool_planner.ts               (CREATE — 17 templates + planPerTool function)
  platform/src/lib/router/__tests__/per_tool_planner.test.ts (CREATE — 15 tests)
  platform/src/app/api/chat/consume/route.ts                 (MODIFY — planPerTool import + stage 3 + 4+idx seqs)
  platform/src/lib/config/feature_flags.ts                   (MODIFY — PER_TOOL_PLANNER_ENABLED added)
  platform/src/lib/trace/types.ts                            (MODIFY — planner_active/tools_refined/tool_count added to TraceDataSummary; StepType NOT modified)

DB changes: none

Cloud Run: amjis-web-00038-mbk → amjis-web-00039-hzk (ACTIVE at 100%)

Tests:
  Before: 1015 passed / 13 failed
  After:  1047 passed / 13 failed
  Delta:  0 new failures

Performance:
  plan_per_tool latency (flag=true, AC.12 smoke): deferred to native smoke
  tools_refined in smoke: deferred to native smoke

Halt-and-report cases: none — all implementation proceeded within may_touch scope

Design notes:
  1. step_type for plan_per_tool uses 'llm' (not 'plan') to avoid breaking TracePanel.tsx (must_not_touch).
     The step semantically IS an LLM step (parallel Haiku calls). The 'plan' label lives in step_name.
  2. perToolOverrides is typed as Map<string, Record<string, unknown>> in route.ts to avoid type collisions
     with ToolPlanOverride's extra fields (keyword, limit, etc.) when spreading into QueryPlan.
  3. synthesisSeq updated from 3+N+1 to 4+N+1 to account for new plan_per_tool at step 3.

Brief status: COMPLETE
Next brief in stream: KARN-W7-R1-EVAL-HARNESS (sequential, Wave 7)
